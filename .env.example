# Backend
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/retail_intel
JWT_SECRET=replace-with-a-long-random-secret
JWT_ALGORITHM=HS256

# For local dev without Postgres, run the backend with:
#   RETAIL_DB_FALLBACK_SQLITE=1 uvicorn app.main:app --reload

# Frontend
VITE_API_BASE=http://localhost:8000
